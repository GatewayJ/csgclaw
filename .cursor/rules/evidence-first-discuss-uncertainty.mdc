---
description: Answer from code/config/logs; stop and ask when uncertain
alwaysApply: true
---

# 有据回答 · 不确定则讨论

## 回答问题

- **优先依据**：本仓库 **代码**（读相关文件、用搜索定位）、**配置**（如 `~/.csgclaw/config.toml`、`defaults/`）、**运行时输出**（终端日志、`make run` / 服务日志、用户粘贴的片段）。
- **结论要写清依据**：重要结论应能追溯到具体文件路径、配置项、函数或日志行；避免只凭猜测或“通常可能”。
- **减少含糊**：若存在多种可能，列出 **已用证据支持的分支** 与 **仍缺少的信息**；不要假装确定。
- **禁止**：在没读代码/配置的情况下给“看起来像”的答案；把推测说成事实。

## 执行任务

- 遇到 **以下情况先停下**，向用户 **简短说明不确定点与可选方案**，请用户确认后再继续：
  - 需求有两种合理理解且会走向不同实现；
  - 需 **`sudo`、删数据、改密钥、动生产** 等有影响的操作且约定不明；
  - 依赖 **外部系统行为**（网关、镜像、网络）而本地无法验证；
  - **`third_party`** 或契约敏感区改动可能影响运行时，而需求未书面确认。

- 能自行核实就 **先查代码/跑针对性命令/读日志**，再动手改。

## 与用户沟通

- 停顿讨论时用 **具体问题**（要选 A 还是 B、是否接受某权衡），避免笼统问“怎么办”。
